# Eval Log — {{LOOP_NAME}}

> Step ⑥ Eval-driven improvement. A loop you never measure is a loop you can't
> improve. Append one row per cycle, then review weekly: tune the checker's
> criteria and the maker's prompt based on rejection patterns and verifier
> accuracy trends.

## How to use
After each run cycle, append one row below. Review weekly. If the checker keeps
rejecting for the same reason, fix the maker prompt. If the checker passed bad
output, tighten its criteria (false negative — checker approved work it should
have refuted). If the checker rejected good output, loosen its criteria
(false positive — checker refuted work that was actually correct).

## Log

| Cycle | Date | Duration | Cost USD | Term reason | Maker output summary | Checker verdict | Verifier accuracy (running %) | False neg/pos? | Notes |
|-------|------|----------|----------|-------------|----------------------|-----------------|-------------------------------|-----------------|-------|
| 1 |  |  |  |  |  |  |  |  |  |
